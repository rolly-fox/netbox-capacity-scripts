# Templates

Copy **`script_stub.py`** to the repository root with a descriptive name (e.g. `site_audit.py`). Rename the contained class and metadata, implement `run()`, then commit.

Do **not** register this stub unchanged in NetBox—it is placeholder code only.
